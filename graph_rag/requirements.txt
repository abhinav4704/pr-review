tree-sitter>=0.25
tree-sitter-java>=0.23
tree-sitter-python>=0.25
tree-sitter-javascript>=0.23   # .js/.jsx/.mjs/.cjs (JSX-aware)
tree-sitter-typescript>=0.23   # .ts/.mts/.cts + .tsx
neo4j>=6.0
python-dotenv>=1.0     # load .env (REPO_PATH/REPO_NAME/NEO4J_*/GRAPH_RAG_* + provider keys)
anthropic>=0.49        # Phase 2 semantic enrichment, providers: anthropic / bedrock
# semantic provider extras (install only the one you use):
#   bedrock: pip install 'anthropic[bedrock]'   (AWS creds + AWS_REGION)
#   openai:  pip install openai                 (OPENAI_API_KEY)
sentence-transformers>=3.0   # Phase 3 vector leg, local embeddings (default, offline, no key)
# embed provider extras (install only the one you use instead of local):
#   voyage:  pip install voyageai               (VOYAGE_API_KEY)
#   openai:  pip install openai                 (OPENAI_API_KEY)
protobuf>=5.0          # read scip-python's index.scip (Stage 2 precise resolver)
# dev-only (regenerate graph_rag/scip/scip_pb2.py): grpcio-tools
# external tool (Python CALLS resolution): scip-python — install with:
#   npm install --prefix scip_tooling @sourcegraph/scip-python@0.6.6

# web UI (graph_rag/webapp)
fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9

# streamlit frontend (graph_rag/frontend) — GitHub -> Neo4j -> 4-agent analysis UI
streamlit>=1.38
requests>=2.31
